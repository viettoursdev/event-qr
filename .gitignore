node_modules/

# ⚠️ KHÔNG commit dữ liệu khách & file riêng tư lên GitHub
data/*.xlsx
data/*.xls
private/

# Dữ liệu khách dạng tĩnh — KHÔNG đẩy lên repo public (trang QR đọc từ Firestore event_public)
docs/g/*.json

.DS_Store
